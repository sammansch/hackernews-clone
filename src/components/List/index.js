import React from 'react';
import ListItem from '../ListItem';
import { ListWrapper } from './styles';

const List = () => (
    <ListWrapper>
        <ListItem />
    </ListWrapper>
);

export default List;